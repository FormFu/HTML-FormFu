---
auto_fieldset: 1

elements:
  - type: Repeatable

# it's now the default behaviour
#    increment_field_names: 1

    elements:
      - name: foo
      - name: bar
  - type: Submit
    name: submit
